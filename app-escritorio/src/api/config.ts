// La app de escritorio corre en la misma máquina que el backend durante
// desarrollo -- si en algún momento corre contra un servidor remoto, cambiar
// esto (o moverlo a una variable de entorno de build).
export const API_BASE_URL = "http://localhost:8000";
